# 1. Pull the latest from GitHub to avoid conflicts
git pull origin master

# 2. Check what’s pending locally
git status

# 3. Stage all new/modified files
git add .

# 4. Commit (only if there is something to commit)
git commit -m "Update images and final adjustments"

# 5. Push to GitHub
git push origin master